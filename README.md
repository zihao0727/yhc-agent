# 萤火虫报价管理

第一阶段提供 PostgreSQL 价格资料管理；第二阶段提供图片/PDF 客户需求和真实工具调用 Agent Loop，自主查库、判断结果、换条件查询、生成报价草稿，保留人工核实及批准导出。

## 技术与范围

- 前端：TypeScript、Vue 3、Vite、Element Plus。
- 后端：Python、FastAPI、SQLAlchemy、Alembic。
- 数据库：PostgreSQL 18。独立库 `firefly_pricing`，专用非超级用户 `firefly_pricing_app`。
- 已实现：价格增删改查、搜索筛选分页、规则原文管理、来源定位、版本历史、修改审计。
- 删除采用停用，保留来源及历史。编辑需要理由，过期版本提交返回 409。
- 第二阶段已实现：图片/PDF 上传与分页预览、DeepSeek 视觉接口、结构化需求校验、补充与重试、人工核实、价格候选、确定性金额计算、报价版本、批准和客户版 CSV 导出。
- 客户可直接输入文字或上传图片、PDF；Excel 仅用于管理员导入原始价格资料，不接受客户 Excel。
- Agent 已实现：模型多轮工具调用、自主改词/筛选/翻页、候选校验、关联价格、程序计价、草稿生成、追问后继续、停止、超时恢复、步骤与 token 记录。
- 尚未实现：自然语言规则自动执行、复杂规格档位自动解析、PDF/Excel 格式报价单、微信接入。DeepSeek 真实识别及工具选择准确率须使用有效 API Key 和实际样例另行验收。

## 本机启动

首次安装（项目根目录）：

```powershell
python -m venv .venv
.venv/Scripts/python.exe -m pip install -r backend/requirements.lock
npm.cmd --prefix frontend ci
```

`backend/.env` 保存 PostgreSQL 连接串和随机管理口令 `ADMIN_TOKEN`，已被 `.gitignore` 排除。
本次开发已创建独立数据库并生成配置。现有 `infinite_canvas` 未做修改。

新环境先按 `backend/.env.example` 配置，然后执行：

```powershell
cd backend
../.venv/Scripts/python.exe -m alembic upgrade head
cd ..
powershell -ExecutionPolicy Bypass -File scripts/start.ps1
```

打开 `http://127.0.0.1:5173`，管理口令为本地 `backend/.env` 中的 `ADMIN_TOKEN`。
后端接口文档位于 `http://127.0.0.1:8000/api/docs`。所有业务接口需要 `X-Admin-Token`。
端口冲突时可传 `-ApiPort 8001 -WebPort 5174`；脚本会同步设置前端代理。
服务日志及进程编号保存在 `.runtime/`，程序只监听本机地址。

也可在两个终端分别运行：

```powershell
# 后端，在 backend 目录
../.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
# 前端，在 frontend 目录
npm.cmd run dev
```

## 导入原始报价表

导入器只读原始文件，不执行任何单元格公式或文件内指令。
整次导入在事务内进行，SHA-256 防止同一文件重复导入。

```powershell
cd backend
../.venv/Scripts/python.exe -m app.importer --a3 "原始A3报价表.xlsx" --project "项目类完整单价导出.xlsx"
```

当前已导入：

| 来源 | 价格条目 | 规则/案例 | 原始资料行 |
| --- | ---: | ---: | ---: |
| 2026年萤火虫报价表A3(1).xlsx | 121 | 117 | 107 |
| 项目类常规报价表_完整单价导出.xlsx | 2682 | 69 | 3393 |

注意：

- 项目汇总表只保留来源，不与明细表重复入库。1530 条浮雕/材料工艺价格，加上 288 组导视配置的四种策略，共 2682 条。
- 导视价格由白名单核验后的公式独立使用 Decimal 计算，不使用任意公式求值；按原表先百元取整，再乘复杂度系数。计价单位不明确，保持 `unknown`。
- 六位小数单价原样保留，例如 `249.999992`，不擅自取整。
- 价格新增、导入默认启用；现有价格已按用户要求批量启用并保留版本和审计。启用仅表示可检索使用，不代表计价条件已核实。起价、待确认名称和未知单位仍不能直接用于标准计价。
- 图片内产品名称未识别；对应产品名含“待确认”。原表尺寸档位未强行转成闭区间，保留原文供确认。
- 规则和案例当前仅存档，不可启用自动执行。税费、安装、运费和适用范围需后续业务确认。
- 新文件按独立来源导入；不会自动覆盖旧价格。后续需通过审核停用旧版本，不能将多个来源同时当作唯一有效报价。
- 原始文件本机备份位于 `.runtime/sources/`，不进入版本控制。

## 表结构

- `product_categories`、`products`：分类与产品。
- `price_items`：当前价格，使用 Numeric，包含状态和乐观锁版本。
- `price_revisions`：每次价格变动的不可变业务快照。
- `pricing_rules`：产品相关规则、公共说明和钣金案例原文。
- `source_documents`：来源文件指纹、导入时间和导入报告。
- `source_records`：原工作表、行号及单元格内容。
- `change_logs`：操作人、原因、前后快照及时间。
- `alembic_version`：数据库结构迁移版本。

## 验证

```powershell
cd backend
../.venv/Scripts/python.exe -m pytest -q
cd ../frontend
npm.cmd run build
```

后端测试在应用数据库下创建随机 `test_firefly_*` schema，结束后仅删除该测试 schema。
不会向业务表写入测试数据。原文件集成测试默认读取 `.runtime/sources/`，也可设置 `PRICE_SOURCE_DIR`。
`scripts/smoke-ui.cjs` 为浏览器冒烟测试，需要 Node.js、可解析的 `playwright` 包及本机 Chrome；
可通过 `NODE_PATH` 指向已安装的 Playwright。测试涵盖登录、搜索、空结果、表单校验、来源、版本及手机布局，
不修改业务价格。截图写入 `.runtime/screenshots/`。

## 安全与后续

本阶段是单管理员、本机使用，不应直接对公网暴露。上线前需要 HTTPS、实名用户/角色权限、登录限流、备份恢复方案及密钥轮换。
管理口令只保存在后端配置及浏览器当前会话；不会写入前端构建产物。
“仅识别”不向模型发送报价库，不允许模型补造原文事实。Agent 模式经明确授权后向 DeepSeek 发送当前查询得到的候选报价数据及关联规则；允许生成明确标记、待审核的补全参数和费用估算，不允许伪造来源或人工确认状态。最终金额始终由程序计算。

## Agent 自动报价

1. “客户需求”首页现在为对话界面。配置 DeepSeek Key 后，直接输入需求，或附加图片/PDF；勾选外部处理授权后发送，首条消息自动创建会话。
2. PDF 转图片并提取需求。已有明细且没有补充文字时直接复用，不重复发送图片识别。
   输入“继续”“继续查价”“重试”等完整执行口令同样复用已保存明细、价格关联和补全方案；后端也兼容旧页面误传替换标志的请求。有明细时，普通文字通过只读问答接口解释报价或提出局部修改：例如“第3项改为5件”先展示字段差异，确认后才保存并程序重新计价，不重新识别整单。新增附件需明确确认整单重新识别影响；手动重新识别入口仍保留。
3. 模型使用 `search_prices` 自主搜索；根据结果改关键词、材质、状态或页码，不是写死的固定流水线。
4. `evaluate_price` 返回候选原始条件、标准价试算诊断和关联规则，供模型判断，不作为自主估价的准入门槛。新模型工具列表不提供严格标准匹配的 `select_price`；历史关联和人工标准计价路径保留。
5. `calculate_quote` 检查全部需求；`finish_quote` 仅在所有行可计价时生成草稿，单价与金额仍由程序确定性计算。
6. 由模型自主决定查价顺序、是否继续搜索、采用哪个依据和怎样估价，通过 `complete_estimate` 提交方案。未匹配、数量/尺寸不清晰或费用不全，由模型自行估算并在整单结束后列审核清单。程序不预设数量、单价、利润率、税率或来源优先级，也不按暂停原因分类拦截模型。原始字段不被覆盖，补全参数、依据、费用公式及范围单独保存。`read_requirement` 可读取压缩上下文中省略的完整依据和已保存方案。
7. 审核清单提供“修改并重新计价”：修改暂定值、原始需求或费用方案，一键保存并程序重算，无须再次调用模型；保留旧报价版本。批准前须明确确认补全参数、费用、范围及正式商业条款，不自动批准或发送报价。

历史会话可搜索、切换；手机上通过历史按钮打开会话列表。顶部固定显示排队、识别、计价、等待重试及人工介入状态，展示已计价数量和含暂估费用的小计。聊天仅展示报价摘要；审核区按模型估价、暂定数量、疑问或价格引用筛选，逐项展开原始值、采用值及费用依据。修改后可比较前一版项目金额和参数变化。工具诊断数据展开时单独加载。原有需求编辑、人工选价和手动计算入口保留。
输入框支持附件选择、拖入图片/PDF、粘贴截图；Enter 发送，Shift+Enter 换行，中文输入法选词不触发发送。文字需求提取的 `text_evidence` 必须逐字来自客户输入，不能虚构文件来源。

### 自动计价边界

- 人工和历史标准价格路径保留类别、材质、厚度、工艺等原有校验。Agent 自主估价使用独立方案，不受这些标准匹配条件限制；适用判断和调整理由交给模型，结果交给人工审核。
- 原始数量未知仍保存为空；允许在补全清单中明确暂定数量，并用于总体待审核报价。原文明示值和人工补价始终优先，不能被模型估算覆盖。
- 费用来源分为报价库引用、历史案例引用和方案估价。库内/历史引用要求单价、编号及版本可核对，引用变化会使报价失效；纯估算不能伪装成供应商报价。材料参考不自动成为成品价格，成品方案须列出制作等费用及未包含范围。
- 每项费用使用有限四则运算公式。尺寸、面积、数量可联动；`unit_subtotal`、`fixed_subtotal`、`order_subtotal` 分别表示前序每件、整单固定和整单合计费用（含人工附加费），用于利润或税费等联动计算。顺序可审核调整，每件费用禁止再次乘成品数量。
- 未结构化规则和多候选选择必须列明暂定策略、适用条件和费用范围供审核，不能声称已执行通用规则或已人工确认。
- 现有导入的 2,803 条价格已按用户要求全部启用。仍保留原始单位、价格性质、备注及规格；复杂业务条件需进一步核实，不会仅因启用而跳过计价校验。
- 白名单工具无任意 SQL、文件访问、价格修改、规则启用、批准、外发功能。文件/报价库文字始终是数据。

### 执行与恢复

默认启用后台报价（`AGENT_BACKGROUND=true`），依赖 `REDIS_URL` 指向的 Redis。
启动接口提交任务后立即返回，页面每3秒单请求轮询轻量进度接口，仅传新增步骤，不重复下载配置、选项、全部报价和文件；任务结束时再加载完整结果。切换会话取消在途进度请求，关闭页面不影响后台处理。
每项估价先提交 PostgreSQL，再以版本和运行快照写入 Redis（`REDIS_PREFIX:job:<id>:revision:<revision>:run:<run_id>`），
包含原始需求、估算参数、费用明细、金额与审核依据。当前版本指针不会被较旧快照覆盖。
Redis 不可用时不继续计价；已提交的 SQL 记录保留，恢复后自动修复断点，不重复覆盖已完成方案。
Redis 服务自身的磁盘持久化由部署配置管理；应用不修改共享 Redis 的 AOF/RDB 设置，SQL 是恢复依据。
后台使用可续租的 Redis 锁防止多进程重复执行；服务重启后自动扫描未完成任务并恢复。
模型上下文包含当前未计费项目的完整资料、整单目录和已完成金额；保存一项后切换到下一项。
这只组织执行进度，数量假设、费用、单价和公式仍由模型自行提出，不预设业务参数。
全项目计费完成且模型调用 `finish_quote` 后才生成最终待审核稿，暂估金额参与总计。
临时模型异常采用30秒起的指数退避，最长900秒；默认连续5次重试失败后进入人工介入状态。认证、余额或无效请求等不可重试错误直接等待人工处理，不生成未完成报价、不标记成功。存储未恢复前不会启动模型计价。
手动停止仍有效；重新启动需再次授权。后台可能持续产生模型调用费用，可随时停止。
`AGENT_BACKGROUND=false` 仅保留原同步执行兼容模式，不提供后台自动续跑。

`agent_runs` 保存每次执行状态、工具参数/结果、token、追问信息。页面轮询进度，展开步骤可查看查价条件与返回结果。
识别失败单独记录 `extract_requirements_failed`，包含失败阶段、字段位置/页码（可获得时）、调用次数、token消耗及保留行数，不记录模型完整失败正文。继续执行的 `usage.reused_line_count` 记录复用明细数量，识别token为0；旧失败记录不会伪造补填未知诊断。
响应截断、工具数量超限、重复调用编号或参数JSON异常时，整轮不执行，记录不含响应正文的诊断及消耗，从已保存状态自动恢复。单周期连续3次响应异常后延后重试；跨周期累计6次后需要人工介入。工具schema错误返回字段路径及校验类型，不回显错误输入值。程序不会替模型调用 `finish_quote` 宣布完成。
查价每段最多 20 轮模型调用、60 次工具、100,000 累计 tokens 或 600 秒；达到单段预算时进入下一段。上下文达到60,000字符时仅压缩历史，不消耗执行段数。压缩与恢复都携带程序复算的已计费金额和未完成项目。压缩仅在整批工具调用返回后进行，不产生缺失的工具响应。图片识别 tokens 单独统计。
搜索结果精简展示；`evaluate_price` 向模型返回完整候选条件和规则正文，原始工具结果保存在数据库审计记录中。搜索支持空格分词（AND）、工艺和备注匹配。
后台默认每6轮或90秒在完整工具批次结束后主动让出执行权，按最久未调度顺序继续其他任务；在途模型请求仍受模型超时约束。每个运行累计默认最多100万tokens、100个周期、连续30轮无需求方案变化，达到限制后进入人工介入状态，不冒充报价完成。可通过`AGENT_MAX_TOTAL_TOKENS`、`AGENT_MAX_CYCLES`、`AGENT_MAX_IDLE_ROUNDS`、`AGENT_MAX_RETRIES`、`AGENT_SLICE_ROUNDS`、`AGENT_SLICE_SECONDS`配置。同步兼容模式保留8段、30分钟、160轮和480次工具保护。这些是调度保护，不决定业务价格。
模型检查点只保留最近的精简搜索记录和每条需求最近一次核对结果；完整审计步骤仍保存在SQL。每轮提供相同工具集，不替模型指定价格策略；无进展和资源预算只用于停止失控执行并请求人工介入。模型自主调用 `finish_quote` 完成交付；同步兼容模式也可调用 `finish_pending_quote` 保存未完成进度。未知金额为 null，未完成总价为空，不允许批准或正式导出。
停止立即标记本次运行失效，在途调用可能已产生费用，但其返回不再写入需求。服务器崩溃后在保护期结束可恢复。
继续操作会开启一个新运行，复用已保存需求、选价和补充历史，再重新查询验证，不重放旧的模型隐藏推理。
当前为API进程内后台线程配合Redis租约和SQL恢复，尚未引入独立队列worker服务；不能视为生产级无人值守调度系统。报价计算仍在每次检查点完整复核，审计仍使用`agent_runs.steps`；增量计价缓存与独立事件表需要单独迁移及依赖失效验证。

## 第二阶段使用

1. 在“客户需求”右上角的钥匙按钮中配置 DeepSeek API Key。默认模型 `deepseek-flash`，接口为官方 `/chat/completions`；配置保存不代表远端连接已验证。
2. 新建需求，上传 JPG/PNG/WebP 或 PDF。文件先保存在本地，PDF 经 PDFium 按页转成图片。
3. 手动模式点击“仅识别”，明确授权后才会将任务图片、历史需求及补充文字发送给 DeepSeek。
4. 校验提取结果并补齐数量、材质、尺寸、工艺。可在模型追问后补充并重新识别，也可直接手工编辑。重新识别会清除价格关联及确认状态，旧报价失效。
5. 每项需求匹配价格，核对原价档位和相关规则，填写适用性依据。价格默认已启用，但仍需通过单位、价格性质、产品名称等计价校验。
6. 按厘米/米计价时填写“每件计价长度(mm)”，按平方米计价时填写单件宽、高；按件/套计价使用数量。附加费由人工按依据填写，支持每件和整行一次。
7. 填写税费、运费、安装费和有效期条款，计算草稿。存在阻塞时只显示已计价小计，不显示完整总价，也不能批准。
8. 审核并批准后导出客户版 CSV。内部成本、毛利、价格审核说明不进入客户文件。修改需求或相关价格会使旧报价过期，必须重新计算和批准才能再次导出。

### 模型配置与保护

- 可以通过 `DEEPSEEK_API_KEY` 环境变量/`.env` 设置密钥，也可以在页面中配置到 `.runtime/deepseek.json`；环境变量优先，页面不会返回已保存密钥。
- 基础 URL 和模型名只能从后端配置调整，浏览器不能提供任意远端地址。
- 模型输出必须符合 Pydantic schema，禁止额外金额字段；来源文件编号、页码、数值范围均校验。
- 每页识别最多两次模型调用：仅格式或完整性失败时尝试修复一次。多页按页提取后合并，保留原始序号 `source_item`；401、余额不足、限流或网络错误不自动重试。
- 原需求在识别失败时保留。识别有独立运行记录；服务异常留下的“识别中”可在超时保护期后手动恢复。
- 请求超时默认 120 秒，可用 `DEEPSEEK_TIMEOUT` 修改；恢复保护期为两倍超时加 90 秒。
- 客户文件内容一律作为数据，不执行图片/PDF 内的命令或指令。所有提取结果默认未经人工核实；Agent 只能在自动校验通过后生成待审核草稿，不代表人工确认。

### 文件与金额边界

- 单任务最多 6 份文件、20 页、50MB；单个文件最大 20MB；HTTP 请求体另设 52MB 上限。
- 根据实际内容验证格式；不接收客户 Excel、GIF 动图或伪装的脚本文件。图片解码限制为 2500 万像素。
- PDF 逐页渲染并读取文本层，模型逐页接收图片及原文，避免整份密集表格缩图导致串项。模型用预览图片最长边为 1800px。板厚与成品壳深 `depth_mm` 分开，禁止把不同部件厚度相加。有文本层时对提取的尺寸数值做交叉校验，无法找到的数值置空并标注待核实；这不是完整语义正确性保证，图示独有数值/单位换算仍需复核。细密扫描件或模糊资料可能需要局部图。
- 对可识别的编号表格，逐行核对原序号覆盖：漏项补回原文并标记待复核，同行不同尺寸置空并提示冲突，疑似把壳深当板厚也置空。此检查是保守兜底，不能替代对复杂图纸的人工验收。
- 上传资料位于 `.runtime/customer-files/`，预览 API 要求管理身份，不暴露本地文件路径。当前没有自动清理期限和客户资料删除界面，上线前需补全保留与清理策略。
- 所有金额使用 Decimal；每行先计算基础金额和附加费，再按分四舍五入，总金额汇总各行金额。
- 工艺、尺寸档位、起算面积、税费、毛利等业务规则当前仍由人审核适用，不把未确认规则偷偷用于自动报价。

### 第二阶段数据库与测试

新增 `requirement_jobs`、`customer_files`、`extraction_runs`、`quote_drafts`、`agent_runs`，迁移为追加式，不改动既有价格。

```powershell
cd backend
../.venv/Scripts/python.exe -m alembic upgrade head
cd ..
powershell -ExecutionPolicy Bypass -File scripts/restart-api.ps1
```

`scripts/smoke-phase2.cjs` 覆盖真实浏览器里的上传、预览、手工需求、匹配、计算、批准、CSV，以及上传授权后自动查价、空结果改词、生成草稿和移动端。
`scripts/run-phase2-ui.ps1` 在 8001/5174 启动独立测试服务，并在随机测试 schema 中运行；结束后停止本次启动的服务并删除测试 schema。运行前执行 `scripts/create-phase2-fixture.py` 生成样例，且让 Node 能解析 Playwright。
测试只使用模拟模型响应，不向 DeepSeek 发送客户资料或消耗真实 API 额度。
